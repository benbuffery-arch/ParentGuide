# UK Parent Guide — Bristol & Somerset (v2)
- Leaflet mini map modal on card click
- Filters: area (postcode prefix), category, toggles (events/classes/parks/indoors/outdoors), distance slider in 5-mile steps with user postcode
- Geocoding via Nominatim (OSM) with localStorage cache
- Logo embedded and theme refined
